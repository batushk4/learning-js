// const soma = function(v1, v2){return v1+v2}

const soma = (v1, v2) => {return v1 + v2}

const nome = n => {return n} //se receber apenas um parêmetro, não precisa colocar os ()

const add = n => n + 10 // não precisa colocar o return, se for algo simples

console.log(soma(10, 5))
