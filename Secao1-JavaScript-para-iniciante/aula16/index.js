// manter o array conciso e tentar manter apenas um tipo de dado dentro dele
//arrays são indexados por elemento
//                 0       1        2         
//const alunos = ['Luiz', 'Maria', 'João'];
//console.log(alunos[1]); //chamar um array

/*
const alunos = ['Luiz', 'Maria', 'João'];
alunos[2] = 'Eduardo' //mudar um elemento no array
console.log(alunos); 
alunos[3] = 'Luiza'; // adicionar no array, mas dessa maneira pode ser meio complicado

alunos[alunos.length] = 'Luiza';
alunos[alunos.length] = 'Rafael';
alunos[alunos.length] = 'Renato'; //adiciona ao array

alunos.push('Eduardo'); 
alunos.push('Oséias'); // já adiciona ao array sem precisar de mais nada, porém fica adicionado no final

alunos.unshift('Luiza'); //adiciona no começo
alunos.push('Oséias'); // já adiciona no final 

alunos.pop(); // remove o elemento do final
alunos.shift(); // remove o elemento inicial

const removido = alunos.pop(); //guarda o elemento removido numa variável

delete alunos[1]; //deleta e um item fica vazio

console.log(alunos.slice(0 , -2)); // ele mostra sem os 2 últimos

console.log(typeof alunos); //ele mostra que isso não é um array, mas sim um objeto
console.log(alunos instanceof Array); // mostra que é um array

*/

const alunos = ['Luiz', 'Maria', 'João'];






